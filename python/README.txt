In this folder, all the code for this project can de found.

The scripts run in the main.py scripts are the important ones. The other ones are either supprt scripts (like the ApisDataset.py script, which contains the ApisDataset class), or scripts used for trying out snippets of code.

In order to run these scripts, download the Scripts.zip file, unpack and run main.py.
